# Music_Tagging_AI

## Introduction & Use Case

본 프로젝트는 **"유튜브 영상의 비정형 메타데이터를 검색창에 그대로 입력했을 때, 사용자가 의도한 핵심 키워드(아티스트 및 곡명)를 정확히 정제하여 연관검색어나 자동완성을 추천해 줄 수 없을까?"** 라는 아이디어에서 출발했습니다.

유튜브 영상 제목은 국영문 병기, 플랫폼 노이즈, 특수문자 등이 섞여 있어, 사용자가 제목 전체를 복사해 검색창에 넣으면 기존 시스템은 노이즈까지 검색어로 인식하는 한계가 있습니다. 

본 모델은 이러한 문제를 해결하기 위해, 입력된 원본 메타데이터에서 **핵심 정보인 `ARTIST`와 `SONG`만을 정밀하게 격리 및 태깅**합니다. 이를 통해 다음과 같은 실제 서비스 기능을 구현할 수 있습니다:

* **정밀한 연관검색어 및 검색 자동완성 제공**: 노이즈(`[Official]`, `Cover` 등)를 제외한 핵심 개체명만 추출하여 사용자에게 깔끔한 추천 검색어 제시
* **검색 쿼리 전처리 엔진**: 지저분한 Raw 텍스트에서 불필요한 토큰을 걷어내고 핵심 키워드로만 검색 인덱싱을 수행하여 검색 엔진의 정확도 향상

<br>

## Overview
이 프로젝트는 유튜브 영상 제목 및 채널명 텍스트에서 아티스트(ARTIST)와 원곡명(SONG)을 자동으로 추출하고 태깅하는 개체명 인식(NER) 모델을 개발하는 것을 목표로 합니다.

규칙 기반(정규식) 자동화 레이블링의 치명적인 한계와 지표의 착시를 극복하기 위해, **전체 2,192건의 커스텀 데이터셋을 수작업으로 직접 구축(Manual Cleaning & Labeling)** 하였습니다. 나아가 데이터 특성을 고려한 전처리 파이프라인 최적화를 통해 실제 서비스 환경에 준하는 고성능 모델을 구현하였습니다.

| 지표 | ARTIST | SONG | Overall(Weighted Avg.) |
|------|--------|------|------------------------|
| **Precision** | 90% | 87% | 89% |
| **Recall** | 94% | 92% | 93% |
| **f1-score** | 92% | 89% | 91% | 

<br>

## Human-Verified Strict Test

본 프로젝트는 테스트셋 내의 토큰 단위 F1-Score가 주는 수치적 착시를 배제하고, **실제 프로덕션 환경에서의 체감 성능을 검증하기 위해 인간이 전수 검수한 100개의 무작위 유튜브 제목 데이터로 실측 평가**를 진행했습니다.

* **평가 방식**: 아티스트와 원곡명을 단 한 글자의 오차나 노이즈 유입 없이 완벽하게 격리해 낸 경우만 '정답'으로 인정하는 엄격한 기준(Strict Match) 적용.

| 평가 대상 모델 | ARTIST (아티스트) 적중률 | SONG (원곡명) 적중률 | 비고 / 실전 체감 품질 |
| :--- | :---: | :---: | :--- |
| **정규식 자동화 모델** | **3%** (3 / 100개) | **4%** (4 / 100개) | 규칙을 벗어난 Wild Text에서 오탐으로 실사용 불가능 |
| **최종 파인튜닝 모델 (In-Domain)** | **93%** (93 / 100개) | **91%** (91 / 100개) | 100곡 중 90곡 이상 자동 추출 가능 |

### 정규식 자동화의 함정과 데이터 무결성(Data Integrity) 검증
초기 실험 시 정규식 패턴 매칭 기반으로 학습/테스트셋을 자동 생성해 돌렸을 때는 수치상 F1-Score가 97%에 육박하는 기현상이 발생했습니다. 그러나 이는 정규식이 오탐한 결과가 정답지에도 똑같이 반영되어 발생한 
**'순환 참조(Circular Dependency)로 인한 지표의 착시'**
였습니다. 

실제 Raw 텍스트를 입력하자 정규식 모델은 특수문자를 유입시키거나 코어 텍스트를 누락하며 **3~4%**이라는 낮은 적중률를 보였습니다. 반면, 본 프로젝트의 수작업 정제 모델은 90% 이상의 적중률로 높은 실전 일반화 성능을 보였습니다.

<br>

## Tagging Guideline
데이터의 일관성을 유지하고 모델이 올바른 문맥적 경계를 학습할 수 있도록, 아래와 같은 명확한 수작업 가이드라인을 수립하여 2,192건의 데이터를 정제하고 BIO(Beginning-Inside-Outside) 태깅 스키마를 적용했습니다.

### 개체명별 태깅 범위 정의
* **ARTIST (아티스트)**
  * 오직 아티스트의 고유 활동명만 포함 (조사, 소속사 표기, `[Official]` 등 플랫폼 노이즈 제외).
  * 국문/영문 병기 시 (예: `뉴진스(NewJeans)`) 해당 아티스트의 국적(주 활동 국가)만 인정
* **SONG (원곡명)**
  * 곡의 순수 제목만 포함 (리믹스, 라이브, 커버 라이팅 부가 정보인 `(Prod. By ~)`, `[Live]`, `Cover` 등은 태그에서 제외).
    
### 데이터 처리 및 태깅 예시

모델의 입력 구조와 BIO 태깅 스키마가 실제로 어떻게 적용되는지 보여주는 대표적인 예시입니다.

#### 예시 1) 기존 데이터셋 패턴 (SONG - ARTIST)
* **Raw Data (원본 메타데이터)**
  * `Title (영상 제목)`: "밤편지 (Through the Night) - IU"
  * `Channel Name (채널명)`: "이지금 [IU] - Topic"
* **Model Input (콤마 `,` 구분자가 도입된 전처리 시퀀스)**
  * `Input`: "밤편지 (Through the Night) - IU , 이지금 [IU] - Topic"
* **Model Output (최종 BIO 태깅 결과)**
  * 밤편지(`B-SONG`) (`O`) (`O`) (`O`) -(`O`) IU(`B-ARTIST`) ,(`O`) 이지금(`O`) [`O`] IU(`O`) -(`O`) Topic(`O`)
  * *주석: 가이드라인에 따라 곡명 뒤의 부가 정보 `(Through the Night)` 및 채널명의 플랫폼 노이즈는 태깅에서 제외됩니다.*

#### 예시 2) OOD 데이터셋 패턴 (ARTIST - SONG)
* **Raw Data (원본 메타데이터)**
  * `Title (영상 제목)`: "NewJeans (뉴진스) 'Ditto' Official MV"
  * `Channel Name (채널명)`: "HYBE LABELS"
* **Model Input (전처리 시퀀스)**
  * `Input`: "NewJeans (뉴진스) 'Ditto' Official MV , HYBE LABELS"
* **Model Output (양방향 데이터 증강 적용 후 정상 예측 결과)**
  * NewJeans(`O`) (`O`) 뉴진스(`B-ARTIST`) (`O`) 'Ditto'(`B-SONG`) Official(`O`) MV(`O`) ,(`O`) HYBE(`O`) LABELS(`O`)
  * *주석: 국영문 병기 시 주 활동 국가의 언어인 `뉴진스`만 ARTIST로 인정하며, 곡명인 `'Ditto'`를 정확히 격리합니다.*
<br>

## Dataset & Language Analysis
모델 학습 및 검증에 사용된 커스텀 데이터셋(총 2,192곡)의 메타데이터 열(Column)별 언어 구성 비율입니다. 텍스트 내 문자 패턴을 기준으로 정밀 분류하였습니다.

| Language Category | Title (영상 제목) | Original Title (원곡명) | Artist (아티스트) | Channel Name (채널명) |
| :--- | :---: | :---: | :---: | :---: |
| **English (영어/로마자)** | 62.23% | 64.92% | 90.69% | 91.10% |
| **English + Korean (혼용)** | 12.45% | 0.05% | 0.05% | 3.24% |
| **English + Japanese (혼용)** | 9.63% | 0.18% | 0.27% | 2.37% |
| **Japanese (일본어 단독)** | 7.57% | 16.10% | 4.84% | 1.92% |
| **Korean (한국어 단독)** | 6.61% | 17.61% | 3.01% | 0.36% |
| **European (유럽권 악센트)** | 0.50% | 0.41% | 0.91% | 0.91% |
| **English + Korean + Japanese** | 0.32% | 0.00% | 0.00% | 0.00% |
| **Russian (러시아어)** | 0.05% | 0.05% | 0.05% | 0.05% |
| **Thai (태국어)** | 0.05% | 0.05% | 0.00% | 0.00% |
| **Others (숫자 및 기호)** | 0.59% | 0.64% | 0.18% | 0.05% |

* **다국어 모델(Multilingual BERT) 선정 배경**: 아티스트 및 채널명은 글로벌 공식 표기를 따라 영어(로마자) 비중이 90% 이상으로 압도적이지만, 실제 영상 제목과 원곡명에서는 한국어, 일본어뿐만 아니라 클래식 곡의 유럽권 악센트 부호, 러시아어, 태국어까지 다양하게 교차 출현하는 특징을 보입니다. 이에 따라 단일 언어 모델이 아닌 다국어 임베딩 표현력이 뛰어난 `bert-base-multilingual-cased`를 최종 베이스 모델로 채택하였습니다.

<br>

## Technical Deep Dive & 주요 엔지니어링 허들 해결

### 1. Data-Centric 접근을 통한 SONG 태그 병목 현상 해결 (60%대 -> 91%)
* **문제점 & 원인 분석**: title (영상 제목)과 channel_name (채널명) 두 가지 독립된 메타데이터를 결합하여 입력하는 과정에서, 모델이 두 필드의 경계를 인지하지 못하는 현상이 발생했습니다. 특히 channel_name이 [아티스트] - Topic 형태일 때, 모델은 후반부의 패턴을 보고 ARTIST는 90% 이상 높은 확률로 적중시켰으나, 경계가 모호해진 title 영역의 SONG은 일반 단어로 오탐하며 60%대의 저조한 성능을 보였습니다.
* **해결책** (컨텍스트 전환점 , 도입): 두 필드를 결합할 때 중간에 **구분자 콤마(,)를 삽입하여 명확한 메타데이터 경계 마커(Boundary Marker)**를 제공했습니다.
* **효과**: 콤마가 명시적인 Boundary Marker 역할을 제공하여 모델이 title과 channel_name을 보다 쉽게 구분할 수 있도록 하였고, 그 결과 SONG 적중률이 91%까지 상승하였다.

### 2. 클래스 불균형에 대응하는 커스텀 손실 함수 구현
개체명 인식 작업의 특성상 대부분의 토큰은 "O" 태그를 가지므로, 데이터에 심각한 클래스 불균형이 존재했습니다.
* **기법**: TensorFlow/Keras에서 `masked_sparse_categorical_crossentropy`라는 커스텀 손실 함수를 구현했습니다.
* **마스킹**: -100 레이블을 가지는 패딩 및 서브워드 토큰을 손실 계산에서 제외했습니다.
* **클래스 가중치**: 'O' 태그에 낮은 가중치(`0.1`)를, 그 외 개체명 태그에 높은 가중치(`5`)를 할당하여 모델이 개체명 예측에 더 집중하도록 유도했습니다.
* **최종 가중치**: [0.1, 5, 5, 5, 5]

<br>

## Boundary Marker 실험

| Model | 실성능 | 비고 |
|---------|---------:|---------|
| **Baseline** | 60% | 단순 공백으로 결합 |
| **SEP** | 72% | SEP Token과 Segment Embedding을 활용 |
| **Comma** | 91% | Comma(,)로 결합 |
| **Comma + SEP** | 92% | SEP Token과 Segment Embedding을 활용 및 Comma(,) 결합 |

<img width="2400" height="1500" alt="Image" src="https://github.com/user-attachments/assets/216a5b64-ee49-417c-bc71-3ffe9d82f6c8" />

* ### 분석

  * SEP Token과 Segment Embedding을 활용하여 title과 channel_name의 경계를 명시적으로 제공했을 때실성능이 60% → 72%로 상승하였습니다.
    다만 오탐 사례 분석 결과, 영문 제목이 병기된 데이터에서 예측 실패가 집중적으로 발생하였습니다.
    반면 Comma Boundary Marker는 국영문 병기 데이터에서도 높은 강건성을 보이며 91%의 실성능을 기록하였습니다.

  * 본 프로젝트의 태깅 가이드라인은 주 활동 국가 언어만 정답으로 인정하였고, 이 기준에서는 72%를 기록하였습니다.
    다만 국문/영문 병기를 모두 허용하는 완화된 기준에서는 94% 수준의 적중률을 보였습니다.
    이처럼 정석적인 방식인 SEP Token과 Segment Embedding 방식의 성능은 충분히 높지만 가이드라인 및 정답 처리의 강건성으로 인해 Comma가 최종 모델로 선정되었습니다.

  * Comma와 Comma + SEP의 모델은 서비스 편의성을 고려하여 선정을 하였습니다.
    실제 검색창에서의 한정된 입력에서는 title과 channel_name라는 메타데이터를 분리하여 입력할 수 없고 합쳐서 입력하는 것의 **편의성**이 압도적으로 높기 때문에 Comma를 최종 모델로 선정하였습니다.

* ### 희소 문자 마커(`|`) 대조 실험 결과
  *  콤마 대신 희소 문자인 파이프(`|`) 단일 마커를 주입하는 실험도 추가로 진행하였습니다. 실측 결과 **86%** 의 성능을 기록하며 `[SEP]` 단일 모델(72%)보다는 우수했으나, 영문 병기 표기 영역에서 토큰 간 엉킴 현상이 관측되어 최종 모델에서는 제외하였습니다. 원인에 대해서는 추가 분석이 필요할 것 같니다.
    
* ### 차기 실험 가설 (Next Experiment Logic)
  * **도메인 특화 스페셜 토큰 최적화**: 단순히 문자열을 더하는 것을 넘어, 다국어 BERT 토크나이저에 `[CHANNEL]` 고유 토큰을 추가하고 임베딩 레이어를 미세 조정하여 메타데이터 전환을 구조적으로 임베딩하는 실험을 준비 중입니다.


<br>

## Out-of-Distribution (OOD)

학습 데이터셋에 포함되지 않았던 **완벽히 새로운 도메인(K-Pop 아이돌 뮤직비디오)의 100개 샘플**을 투입하여 모델의 범용 일반화 성능을 검증하였습니다.

* **실측 결과**: ARTIST 적중률 **61%**, SONG 적중률 50%로 성능 하락 관측.

### 핵심 오탐 원인 분석 (Root Cause Analysis)
* **데이터 과적합**: 기존 데이터셋에서 'title'에 곡명과 아티스트가 모두 들어가 있는 경우 '원곡명 - 아티스트' 형태가 많아 OOD 데이터셋에서 많았던 '아티스트 - 원곡명' 형태에서 높은 오탐률을 보였습니다.

### 향후 개선 방향 (Next Action Plan)
* **양방향 데이터 증강 (Bidirectional Data Augmentation)**: 기존 데이터셋의 구조적 순서를 기계적으로 반전시킨 `[아티스트] , [곡명]` 데이터를 학습셋에 50:50 비율로 추가 주입할 예정입니다. 이를 통해 모델이 특정 위치에 종속되지 않고 개체명 고유의 특징과 문맥을 기반으로 양방향 탐지가 가능하도록 범용성을 고도화할 계획입니다.

<br>

## Tech Stack
 * **Language** : Python 3.11
 * **Deep Learning Framework** : TensorFlow 2.15, Keras
 * **NLP Library** : Hugging Face transformers
 * **Evaluation** : seqeval
 * **Model** : bert-base-multilingual-cased

<br>

## 모델 사용

![music_tagging_ai](https://github.com/user-attachments/assets/4fc8cf7c-7378-45b2-9e5a-a07684058976)

<br>

## Conclusion & Takeaways

### 1. 핵심 성과 요약
* **Data-Centric 접근을 통한 성능 극대화**: 무조건적인 모델 스케일업 대신, 2,192건의 데이터를 수작업으로 직접 정제하고 메타데이터 간 전환점(Comma 마커)을 제공하는 전처리 파 파이프라인 설계를 통해 실측 적중률을 **60%에서 91%까지 끌어올렸습니다.**
* **실전 지표 착시 극복**: 단순 토큰 단위 F1-Score가 주는 수치적 착시(97%)에 안주하지 않고, Human-Verified Strict Match(100% 완전 일치)라는 엄격한 실전형 평가 기준을 도입하여 정규식 모델의 한계(3~4%)를 증명하고 최종 모델의 일반화 성능을 투명하게 검증했습니다.

### 2. 프로젝트의 한계 및 향후 개선 방향 (Next Action Plan)
* **OOD 데이터에 대한 강건성 확보**: 학습 데이터셋의 도메인 편향('원곡명 - 아티스트' 형태)으로 인해 K-Pop 아이돌 MV 등 완벽히 새로운 패턴(OOD)에서는 성능 저하가 관측되었습니다. 이를 해결하기 위해 **양방향 데이터 증강(Bidirectional Data Augmentation)** 을 적용하여 특정 구조에 종속되지 않는 범용 모델로 고도화할 예정입니다.
* **도메인 특화 스페셜 토큰 도입**: 문자열 구분자(,)를 넘어서, 다국어 BERT 토크나이저에 `[CHANNEL]`과 같은 고유 스페셜 토큰을 추가하고 임베딩 레이어를 미세 조정하는 실험을 준비 중입니다.

### 3. 최종 결론 및 기대 효과
본 프로젝트를 통해 개발된 AI 모델은 단순히 비정형 텍스트를 파싱하는 것을 넘어, **"검색창에 메타데이터를 그대로 입력했을 때 노이즈를 걷어내고 정밀한 연관검색어나 자동완성을 추천하는 엔진"** 으로 실제 서비스 UX 개선에 활용될 수 있습니다. 

정형화되지 않은 현실의 Wild Text 데이터 문제를 '데이터 무결성(Data Integrity)' 관점에서 집요하게 분석하고 해결해 나가는 과정을 통해, 엔지니어로서 AI 모델의 성능은 결국 양질의 데이터와 도메인에 맞는 구조적 힌트(Marker)의 설계에 달려있음을 깊이 깨달았습니다.
